#!/usr/bin/env zsh
set -euo pipefail

session_name=${1:?session name is required}

if [[ -n ${TMUX:-} ]]; then
  exec sesh connect --switch "$session_name"
else
  exec sesh connect "$session_name"
fi
