#!/usr/bin/env zsh
set -euo pipefail

cd "${HERDR_ACTIVE_PANE_CWD:-$PWD}"
selected="$(vlm files)"
[[ -n "$selected" ]] || exit 0

"${HERDR_BIN_PATH:-herdr}" pane send-text "$HERDR_ACTIVE_PANE_ID" "@${selected}"
