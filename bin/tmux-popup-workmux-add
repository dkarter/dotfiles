#!/usr/bin/env zsh
set -euo pipefail

script_dir=$(cd -- "$(dirname "${BASH_SOURCE[0]}")" && pwd)
command="${script_dir}/tmux-workmux-add"

tmux display-popup -C >/dev/null 2>&1 || true
exec tmux display-popup -E -w 40% -h 20% "$command"
