# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

silent: true

tasks:
  plugins:install:
    desc: Installs dependencies for local OpenCode plugins
    cmd: aube install --frozen-lockfile --filter ./config/opencode/tui-plugin
