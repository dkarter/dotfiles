# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: 3

tasks:
  install:
    desc: Installs mise itself
    silent: true
    # https://mise.jdx.dev/getting-started.html
    cmds:
      - cmd: brew install mise
        platforms: [darwin]

      - cmd: |
          if [[ -f /etc/debian_version ]] || command -v apt &>/dev/null; then
            sudo apt update -y && sudo apt install -y gpg sudo wget curl
            sudo install -dm 755 /etc/apt/keyrings
            wget -qO - https://mise.jdx.dev/gpg-key.pub | gpg --dearmor | sudo tee /etc/apt/keyrings/mise-archive-keyring.gpg 1> /dev/null
            echo "deb [signed-by=/etc/apt/keyrings/mise-archive-keyring.gpg arch={{ARCH}}] https://mise.jdx.dev/deb stable main" | sudo tee /etc/apt/sources.list.d/mise.list
            sudo apt update
            sudo apt install -y mise
          elif command -v pacman &> /dev/null; then
            sudo pacman --noconfirm --needed -S mise
          fi
        platforms: [linux]

  sync:
    desc: Updates mise tools (runtimes), mise itself and installs packages for this project
    cmds:
      - task: tools:install
      - task: tools:update

  tools:install:
    desc: Installs/updates all mise based tools
    cmds:
      - cmd: mise install

  tools:extras:prompt:
    desc: Prompt to enable optional mise extras profile
    silent: true
    cmds:
      - cmd: |
          if [[ ! -t 0 ]] || [[ ! -t 1 ]]; then
            echo "Skipping extras prompt (non-interactive shell)."
            exit 0
          fi

          mise install gum

          if mise exec -- gum confirm "Install optional extra mise tools?"; then
            zshrc_local="$HOME/.zshrc.local"
            touch "$zshrc_local"

            if grep -qE '^\s*export\s+MISE_ENV=' "$zshrc_local"; then
              sed -i.bak -E 's|^\s*export\s+MISE_ENV=.*$|export MISE_ENV=extra|' "$zshrc_local"
            else
              printf '\nexport MISE_ENV=extra\n' >> "$zshrc_local"
            fi

            rm -f "$zshrc_local.bak"
            echo "Enabled extras profile via MISE_ENV=extra in $zshrc_local"
          else
            echo "Keeping base mise toolset only."
          fi

  tools:install:with-github-auth:
    desc: Installs gh first, authenticates it, then installs all mise tools
    silent: true
    cmds:
      - cmd: |
          mise install gh

          if ! mise exec -- gh auth status &>/dev/null; then
            echo "GitHub CLI is not authenticated. Starting gh auth login..."
            mise exec -- gh auth login
          fi

          export GITHUB_TOKEN="$(mise exec -- gh auth token)"
          export MISE_GITHUB_TOKEN="$GITHUB_TOKEN"

          if [[ -f "$HOME/.zshrc.local" ]] && grep -qE '^\s*export\s+MISE_ENV=extra\s*$' "$HOME/.zshrc.local"; then
            export MISE_ENV=extra
            echo "Detected MISE_ENV=extra in ~/.zshrc.local, installing extras too."
          fi

          mise install

  tools:update:
    desc: Updates all mise based tools
    cmds:
      - cmd: mise up
        silent: true

  tools:outdated:
    desc: List all outdated tools, including major bumps
    cmds:
      - cmd: mise outdated -l
        silent: true
