# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: 3

silent: true

tasks:
  sync:
    desc: Synchronize homebrew packages
    summary: Updates Homebrew packages
    platforms: ['darwin']
    cmds:
      - brew bundle
