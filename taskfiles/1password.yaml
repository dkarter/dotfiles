# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

tasks:
  install:ssh:agent:
    cmds:
      - cmd: mkdir -p ~/.1password
      - cmd: '[ -e ~/.1password/agent.sock ] || [ -L ~/.1password/agent.sock ] || { [ -S ~/Library/Group\ Containers/2BUA8C4S2C.com.1password/t/agent.sock ] && ln -s ~/Library/Group\ Containers/2BUA8C4S2C.com.1password/t/agent.sock ~/.1password/agent.sock; }'
        platforms: [darwin]
