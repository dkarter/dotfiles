[metadata]
name = "gh-repo-checkout"
description = "Clone one of your GitHub repos into ~/dev"
requirements = ["gh", "git", "bat", "herdr"]

[source]
command = "$HOME/dotfiles/bin/tv-gh-repo-list"
output = "{strip_ansi|split:\t:0}"
ansi = true

[preview]
command = "$HOME/dotfiles/bin/tv-gh-repo-preview '{strip_ansi|split:\t:0}'"
env = { BAT_THEME = "ansi" }

[ui]
preview_panel = { header = "{strip_ansi|split:\t:0}" }

[keybindings]
enter = "actions:checkout"

[actions.checkout]
description = "Clone selected repo to ~/dev"
command = "zsh -lc 'mkdir -p \"$HOME/dev\"; repo=\"{split:\t:0}\"; name=\"${repo##*/}\"; target=\"$HOME/dev/$name\"; if [ -d \"$target/.git\" ]; then cd \"$target\" && git pull --ff-only; else gh repo clone \"$repo\" \"$target\"; fi; herdr-open-dev-dir \"$target\"'"
mode = "execute"
