# This file contains the local GitHub App Client ID. The Client ID is not a
# secret, but keeping the file local lets its owner manage it without agents
# inspecting credential-related values.
ghtkn.yaml
