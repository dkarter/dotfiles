#!/usr/bin/env zsh
set -euo pipefail

herdr_bin="${HERDR_BIN_PATH:-herdr}"
plugin_id="${HERDR_PLUGIN_ID:-dotfiles.command-palette}"

exec "$herdr_bin" plugin pane open --plugin "$plugin_id" --entrypoint palette --placement overlay --focus
