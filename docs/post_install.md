# Post-Install Instructions

Now that you have everything installed, it might be a good idea to restart your shell, Herdr, and Ghostty.

For future syncs, after pulling updates from the repo use `task sync` which is lighter and faster.
